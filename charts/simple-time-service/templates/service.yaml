apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-time-service.fullname" . }}
  labels:
    {{- include "simple-time-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "simple-time-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP