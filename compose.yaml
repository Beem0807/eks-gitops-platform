services:
  simple-time-service:
    build:
      context: ./app
      dockerfile: Dockerfile
    image: simple-time-service:local
    container_name: simple-time-service
    ports:
      - "8080:8080"
    environment:
      PYTHONUNBUFFERED: "1"
    restart: unless-stopped